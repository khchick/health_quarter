<div class="firstElement"></div>
    <!--This is the template for the load in page-->
    <template id="loading template">
        <h1>Health Quarters</h1>
        <h2>Recipe finding app</h2>
        <p>If you become a member you can save your most enjoyable dishes for later</p>

        <p>
            <button class="styled" id="search-cal" name="search-cal">Search by Calorie</button>
        </p>
        <p>
            <button class="styled" id="search-tag" name="search-tag">Search by Tag</button>
        </p>
          <p>
            <button class="styled" id="search-ing" name="search-ing">Search by Ingredients</button>
        </p>

    </template>

    <!--Search by calorie-->

    <template id="search-cal">
        <div class="ui block">
            <div class="ui grid">
                <h2 class "page-header">Search for Recipes:</h2>
                <form action="/searchcal" method="post">
                    <div class="ui form">
                        <div class="field">
                            <label>Serch by Calorie:</label>
                            <br> Minimum Calories:
                            <input type="text" name="minCalorie" value="150">
                            <br> Maximum Calories:
                            <input type="text" name="maxCalorie" value="600">
                            <br> Number of search requests (between 1-5):
                            <input type="number" name="quantity" min="1" max="5">
                        </div>
                    </div>
                    <br>
                    <p>
                        <button name="submit" class="ui recipe button" type="submit">Find Recipe(s)</button>
                    </p>
                    <br>
                    <br>
            </div>
            </form>
        </div>
        </div>
    </template>


    <!--Search by tag-->
    <template id="search-tag">
        <div class="ui block">
            <div class="ui grid">
                <h2 class "page-header">Search for Recipes:</h2>
                <form action="/searchtag" method="post">
                    <div class="ui form">
                        <div class='field'>
                            Number of search requests (between 1-5):
                            <input type="number" name="quantity" min="1" max="5"> Queries:
                            <br>
                            Popular searches include: vegetarian, vegan, cheap, popular, sustainable, glutenFree, veryHealthy
                            <br>
                            <input type="text" name="tag1" value="">
                            <input type="text" name="tag2" value="">
                            <input type="text" name="tag3" value="">
                            <input type="text" name="tag4" value="">
                            <input type="text" name="tag5" value="">
                        </div>
                    </div>
                    <br>
                    <p>
                        <button name="submit" id="findRecipe" class="ui recipe button" type="submit">Find Recipe(s)</button>
                    </p>
                    <br>
                    <br>
            </div>
            </form>
        </div> 
        </div>
    </template> 

    <template id="search-ing">
        <div class="ui block">
            <div class="ui grid">
                <h2 class "page-header">Search for Recipes:</h2>
                <form action="/searching" method="post">
                    <div class="ui form">
                        <div class='field'>
                            Number of search requests (between 1-5):
                            <input type="number" name="quantity" min="1" max="5"> Queries:
                            <br>
                            Search ingredients that you have
                            <br>
                            <input type="text" name="tag1" value="">
                            <input type="text" name="tag2" value="">
                            <input type="text" name="tag3" value="">
                            <input type="text" name="tag4" value="">
                            <input type="text" name="tag5" value="">
                        </div>
                    </div>
                    <br>
                    <p>
                        <button name="submit" id="findRecipe" class="ui recipe button" type="submit">Find Recipe(s)</button>
                    </p>
                    <br>
                    <br>
            </div>
            </form>
        </div>
        </div>
    </template> 

    <!--when we want to generate recipes.
    <table class="table" id="recipe_table">
        <tr>
        <th>Recipe Name:</th>
        <th>Total Calories:</th>
        <th>Image:</th>
        <th>Learn more button:</th>
        <th>Favourite:</th>
        </tr>
        </table>-->

    <!--Frist scripts are for rendering the first template, then script give buttons functionality-->
    <script src='https://code.jquery.com/jquery-2.2.4.min.js'></script>
    <script>
        const supportsTemplate = function () {
            return 'content' in document.createElement('template');
        }

        document.addEventListener('DOMContentLoaded', () => {
            if (supportsTemplate()) {
                console.log('Templates are supported.');
                let first = document.getElementById('loading template');
                let content1 = first.content;
                document.body.appendChild(content1);
            } else {
                console.log('ffs')
            }
        })


        //hide both buttons won't work.
        $(document).on("click", "#search-cal", function () {
            console.log('calsearch');
           // $('#pfRecipes').hide;
            $(this).hide();
            $('input[name="search-tag"]').hide(); $('#recipes').hide(); $('#search-cal').hide(); $('#search-ing').hide();
            let second = document.getElementById('search-cal');
            let content2 = second.content;
            document.body.appendChild(content2);
        });

        $(document).on("click", "#search-tag", function () {
            console.log('tagsearch');
            //$('#pfRecipes').hide;
            $(this).hide();
            $('input[name="search-cal"]').hide(); $('#recipes').hide();  $('#search-tag').hide(); $('#search-ing').hide();
            let third = document.getElementById('search-tag');
            let content3 = third.content;
            document.body.appendChild(content3);
        });

          $(document).on("click", "#search-ing", function () {
            console.log('ingsearch');
            //$('#pfRecipes').hide;
            $(this).hide();
            $('input[name="search-cal"]').hide(); $('#recipes').hide();  $('#search-tag').hide(); $('#search-cal').hide();
            let fourth = document.getElementById('search-ing');
            let content4 = fourth.content;
            document.body.appendChild(content4);
        });
        </script>